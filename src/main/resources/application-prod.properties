# Production overrides for cookie/security
app.cookie.http-only=true
app.cookie.secure=true
app.cookie.same-site=None
app.cookie.max-age=604800

# Reminder: ensure spring.profiles.active=prod is set in prod environment (or via runtime env)
# Example: set your production frontend origin(s) here (comma-separated)
# app.cors.allowed-origin-patterns=https://myapp.example.com,https://www.myapp.example.com

# GitHub OAuth (prod)
# Values must be provided via environment variables (e.g., systemd Environment=...)
spring.security.oauth2.client.registration.github.client-id=Ov23livC3tLL1Bza5xF9
spring.security.oauth2.client.registration.github.client-secret=39d3ab1ccb1c7e4ce0c9155761eae931d4f5cdbb
spring.security.oauth2.client.registration.github.scope=user:email
spring.security.oauth2.client.registration.github.redirect-uri=https://ada-back.com/api/auth/github/callback

# IMPORTANT: behind Nginx(SSL termination), honor X-Forwarded-* headers so {baseUrl} is computed correctly.
server.forward-headers-strategy=framework 
