package com.ada.proj.enums;

/**
 * 프로젝트 전역 에러 코드 정의.
 */
public enum ErrorCode {
    USER_NOT_FOUND,
    INVALID_PASSWORD,
    TOKEN_EXPIRED,
    TOKEN_INVALID,
    DUPLICATE_EMAIL,
    NOT_FOUND,
    FORBIDDEN,
    UNAUTHENTICATED,
    VALIDATION_ERROR,
    BAD_REQUEST,
    CONFLICT,
    AUTH_FAILURE,
    INTERNAL_ERROR,
    OAUTH_NOT_CONFIGURED,
    USER_BANNED,
    ALREADY_BANNED,
    BAN_EXPIRES_INVALID
}
